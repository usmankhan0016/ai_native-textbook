"""Database layer module for RAG Agent Backend (Feature 007).

This module handles:
- Neon Serverless Postgres connection pooling (connection.py)
- Database models for sessions, messages, selected_text_metadata (models.py)
- CRUD operations via repositories (repositories.py)
"""
